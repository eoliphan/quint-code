{
  "name": "@haft/tui",
  "private": true,
  "version": "0.0.0",
  "description": "Haft v8 agent TUI — thin client over the agentserver HTTP+SSE wire. Built on @opentui/core + SolidJS.",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "build": "bun run build.ts",
    "lint": "eslint src --max-warnings=0",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@opentui/core": "0.1.105",
    "@opentui/solid": "0.1.105",
    "effect": "^3.21.2",
    "solid-js": "1.9.10"
  },
  "devDependencies": {
    "@types/bun": "1.3.12",
    "typescript": "5.6.3"
  }
}
